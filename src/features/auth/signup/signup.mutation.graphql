mutation register(
  $firstName: String!
  $lastName: String!
  $phone: String!
  $email: String!
  $password: String!
) {
  register {
    firstName: $firstName
    lastName: $lastName
    phone: $phone
    email: $email
    password: $password
  }
}
