mutation acceptInvite($id: Int!) {
  acceptInvite(id: $id) {
    id
    buylist {
      name
    }
    from {
      firstName
      lastName
    }
    to {
      firstName
      lastName
    }
    status
  }
}
